{
  "name": "dsh-tui",
  "version": "0.1.0",
  "description": "Terminal client for DeepSeek Harness: a TUI over the DSH client contract (ctx.remote)",
  "type": "module",
  "main": "lib/index.js",
  "bin": {
    "dsh-tui": "bin/tui.js"
  },
  "files": [
    "lib",
    "bin"
  ],
  "keywords": [
    "dsh",
    "deepseek-harness",
    "tui",
    "terminal",
    "cli"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check lib/index.js && node --check lib/client.js && node --check lib/fold.js && node --check lib/session.js && node --check bin/tui.js",
    "test": "node --test test/fold.test.js test/session.test.js test/client.test.js",
    "test:live": "node scripts/run-live.mjs",
    "flywheel": "node scripts/flywheel.mjs test",
    "flywheel:live": "node scripts/flywheel.mjs test-live"
  }
}
